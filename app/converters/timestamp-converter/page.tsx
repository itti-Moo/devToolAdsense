import{TimestampTool}from'@/components/tool-clients';import{ToolLayout,toolMetadata}from'@/components/tool-layout';export const metadata=toolMetadata('Unix Timestamp Converter | DevKit','Convert Unix timestamps and ISO dates with explicit seconds or milliseconds units.');export default function Page(){return <ToolLayout title="Timestamp Converter" eyebrow="CONVERTERS / DATE & TIME" description="Convert Unix timestamps, ISO dates, and date ranges with clear UTC and local-time output." related={[{href:'/developer-tools/json-formatter/',label:'JSON Formatter'},{href:'/converters/base64-converter/',label:'Base64 Converter'}]}><TimestampTool/><><h2>Unix timestamps explained</h2><p>A Unix timestamp counts elapsed time from 1 January 1970 UTC. Choose seconds or milliseconds explicitly to avoid ambiguous conversions.</p><h2>Local time and UTC</h2><p>Date-time fields use your browser timezone, while ISO output remains unambiguous and can be copied into APIs or logs.</p></></ToolLayout>}

