import{RegexTool}from'@/components/tool-clients';import{ToolLayout,toolMetadata}from'@/components/tool-layout';export const metadata=toolMetadata('Regex Tester | DevKit','Test JavaScript regular expressions, capture groups, replacements, and flags locally.');export default function Page(){return <ToolLayout title="Regex Tester" eyebrow="DEVELOPER TOOLS / REGEX" description="Test regular expression patterns, inspect matches and capture groups, and try replacements in your browser." related={[{href:'/guides/regex-cheat-sheet/',label:'Regex Cheat Sheet'},{href:'/developer-tools/json-formatter/',label:'JSON Formatter'}]}><RegexTool/><><h2>How regular expressions work</h2><p>A pattern describes text to find. Flags change matching behavior: <code>g</code> finds every match, <code>i</code> ignores case, and <code>m</code> enables line-aware anchors.</p><h2>Capture groups and replacements</h2><p>Parentheses capture parts of a match for inspection or reuse in replacement text. Escape special characters when you need to match them literally.</p></></ToolLayout>}

