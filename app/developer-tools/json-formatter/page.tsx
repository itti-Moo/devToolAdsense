import {JsonTool} from '@/components/json-tool';import {ToolLayout,toolMetadata} from '@/components/tool-layout';
export const metadata=toolMetadata('JSON Formatter and Validator | DevKit','Format, minify, validate, sort, and inspect JSON locally in your browser.');
export default function Page(){return <ToolLayout title="JSON Formatter" eyebrow="DEVELOPER TOOLS / JSON" description="Format, minify, validate, and organize JSON with readable error feedback. Your data stays in the browser." related={[{href:'/developer-tools/jwt-decoder/',label:'JWT Decoder'},{href:'/converters/base64-converter/',label:'Base64 Converter'}]}><JsonTool/><><h2>Format JSON for readability</h2><p>Pretty-print deeply nested objects with consistent indentation, or minify JSON before sending it over a network.</p><h2>Validate and fix common mistakes</h2><p>The validator reports malformed quotes, missing commas, and unexpected tokens so you can locate the problem quickly.</p><h2>Use JSON safely</h2><p>Processing happens locally in your browser. Avoid pasting credentials, API keys, or other sensitive production data into shared devices.</p></></ToolLayout>}

