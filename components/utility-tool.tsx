'use client';
import {useState} from 'react'; import {Copy,RefreshCw} from 'lucide-react';
const labels:Record<string,string>={url:'URL Encoder / Decoder',uuid:'UUID Generator',hash:'Hash Generator'};
export function UtilityTool({slug}:{slug:string}){const title=labels[slug]||'Developer Utility';const[input,setInput]=useState('');const[output,setOutput]=useState('');const[version,setVersion]=useState('v4');const[mode,setMode]=useState('encode');const generate=()=>{if(slug==='uuid'){setOutput(crypto.randomUUID());return}setOutput(mode==='encode'?encodeURIComponent(input):decodeURIComponent(input))};return <div className="workspace p-5 sm:p-7"><h2 className="text-xl font-semibold">{title}</h2>{slug==='uuid'&&<div className="mt-4 flex flex-wrap gap-3"><label className="text-xs font-semibold">UUID VERSION<select value={version} onChange={e=>setVersion(e.target.value)} className="ml-2 rounded-lg border p-2"><option>v1</option><option>v3</option><option>v4</option><option>v5</option><option>v6</option><option>v7</option><option>v8</option></select></label><span className="self-center text-xs text-slate-500">Generate UUIDs locally in your browser.</span></div>}{slug==='url'&&<div className="mt-4 flex gap-2"><button className="secondary-button" onClick={()=>setMode('encode')}>Encode</button><button className="secondary-button" onClick={()=>setMode('decode')}>Decode</button></div>}<textarea className="code-area mt-4" value={input} onChange={e=>setInput(e.target.value)} placeholder={slug==='uuid'?'Select a version, then generate':'Enter input...'}/><div className="mt-4 flex gap-2"><button className="primary-button" onClick={generate}>{slug==='uuid'?'Generate':'Process'}</button><button className="secondary-button" onClick={()=>navigator.clipboard.writeText(output)} disabled={!output}><Copy size={14}/> Copy</button><button className="secondary-button" onClick={()=>{setInput('');setOutput('')}}><RefreshCw size={14}/> Clear</button></div><textarea className="code-area mt-4" value={output} readOnly aria-live="polite" placeholder="Result will appear here"/></div>}



